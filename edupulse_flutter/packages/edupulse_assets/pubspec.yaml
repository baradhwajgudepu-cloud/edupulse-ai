name: edupulse_assets
resolution: workspace
description: Shared image, font, SVG, icon, and Lottie animation resources for EduPulse AI.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1


flutter:
  assets:
    - assets/branding/

