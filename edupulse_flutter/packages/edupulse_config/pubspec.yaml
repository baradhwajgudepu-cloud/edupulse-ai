name: edupulse_config
resolution: workspace
description: Environment parameters, feature flags, base URLs, and flavor flags for EduPulse AI.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^10.1.0
  edupulse_core:
    path: ../edupulse_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
