name: edupulse_localization
resolution: workspace
description: Multi-language translations and localization features for EduPulse AI.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  edupulse_core:
    path: ../edupulse_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
